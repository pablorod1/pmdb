---
import Layout from "@layouts/Layout.astro";
import { getMovieDetails } from "@lib/tmdb";


// const movie = await getMovieDetails(id);

// const { title, release_date, poster_path, vote_average, genres, overview } = movie;
---

<!-- <Layout title={title}>
  <main class="flex flex-col items-center justify-center p-10">
    <div class="flex flex-col items-start gap-4">
      <h1 class="text-4xl font-bold text-white">{title}</h1>
      <img class="w-full rounded-xl" src={`https://image.tmdb.org/t/p/original${poster_path}`} alt={title} />
      <p class="text-gray-300">{overview}</p>
      <p class="text-gray-300">Release Date: {new Date(release_date).toLocaleDateString()}</p>
      <p class="text-gray-300">Rating: {vote_average}</p>
      <p class="text-gray-300">Genres: {genres.map((genre: any) => genre.name).join(', ')}</p>
    </div>
  </main>
</Layout> -->


<Layout title="Test">
  <h1 class="text-black">Test</h1>
</Layout>